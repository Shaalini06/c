# include<stdio.h>
#include<string.h>
int main()
{
    char str[100],sstr[100];
    int i=0,n,j=0;
    printf("\n Enter the string:");
    scanf("%s",str);
    printf("\n Enter the number of char to copy");
    scanf("%d",&n);
    j=strlen(str)-n;
    while(str[j]!='\0')
    {
        sstr[i]=str[j];
        i++;
        j++;
    }
    sstr[i]='\0';
    printf("\n The substring is:");
    puts(sstr);
    return 0;
}
